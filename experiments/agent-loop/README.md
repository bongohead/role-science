# About
This directory contains code for agentic prompt injection usage on the GPT-OSS models.

1. Run `jailbreak-gpt-oss.ipynb` - this will run ReAct tool use loops in Harmony format against a standard web classification task, including both baseline and harmful benchmarks.
2. Run `analyze-activations.ipybn` - this will analyze the actual activations.